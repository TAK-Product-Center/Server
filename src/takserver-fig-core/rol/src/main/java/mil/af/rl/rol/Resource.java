package mil.af.rl.rol;

/*
 * Enum mirroring ROL resources
 * 
 * see ROL grammar (Rol.g4) 
 */
public enum Resource {
    QUERY,
    SUBSCRIPTION,
    ROLE,
    PRIORITY,
    RESOURCE,
    PACKAGE,
    MISSION
}
